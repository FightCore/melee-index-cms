'use strict';

/**
 * character-guide controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::character-guide.character-guide');
